services:
  docs:
    image: papyrus_local_docs
    container_name: papyrus_local_docs
    build:
      context: .
      dockerfile: ./compose/local/docs/Dockerfile
    env_file:
      - ./.envs/.local/.django
    volumes:
      - ./docs:/docs:z
      - ./config:/app/config:z
      - ./papyrus:/app/papyrus:z
    ports:
      - '9000:9000'
    command: /start-docs
